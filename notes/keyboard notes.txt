BASIC KEYBOARD NOTES:::

boolean (true or false value) can store every key on the keyboard.

Every key on the keyboard has in ID attached to it; Called KEY CODE
--A number designating the ID of the key. 

for ex. "8" key has ID of "123", then set an array to that ID! 

And then implement that ID true or false to control input and output. 